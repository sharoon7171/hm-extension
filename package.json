{
  "name": "hotmovies-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "npm run clean && concurrently -k -n options,background,content \"vite build --watch\" \"vite build --config vite.background.config.ts --watch\" \"vite build --config vite.content.config.ts --watch\"",
    "build": "npm run clean && tsc --noEmit && vite build && vite build --config vite.background.config.ts && vite build --config vite.content.config.ts"
  },
  "dependencies": {
    "firebase": "^12.12.1",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "@types/chrome": "^0.1.40",
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.2.4",
    "terser": "^5.46.2",
    "typescript": "~6.0.3",
    "vite": "^8.0.10"
  }
}
